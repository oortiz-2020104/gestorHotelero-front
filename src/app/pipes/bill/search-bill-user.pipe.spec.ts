import { SearchBillUserPipe } from './search-bill-user.pipe';

describe('SearchBillUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBillUserPipe();
    expect(pipe).toBeTruthy();
  });
});
